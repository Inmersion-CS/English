# Contributing to English

Requirements: Node.js 22.13+ and npm.

```bash
git clone https://github.com/Inmersion-CS/English.git
cd English
npm ci
npm run dev
```

Before a pull request run the dependency audit, typecheck, lint, tests, build, and formatting check. Keep learning content clear and natural, changes focused, and interfaces accessible.
